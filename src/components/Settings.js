import React from 'react'
import { ScrollView, Text, View } from 'react-native'

function Settings() {
  return (
    <ScrollView>
        <View>
            <Text>Settings</Text>
        </View>
    </ScrollView>
  )
}

export default Settings